# M4 待办清单（M3 终审裁决产出）

来源：m3-graph-timeline 分支终审（2026-08-27）。M3 待办中的必修项（重入守卫/isError/facts cap）已全部消化。

## 建议修（M4 顺手项）

1. graph.ts 归一化兜底分支补测试：给 fake 加 "无 nodes / paths 键 / from-to 键" 形状变体，覆盖六条防御路径（真实 traverse_graph 形状以 M3 验收清单第 2 条实测为准，不符则先修归一化层）。
2. graph.ts direction 白名单（["in","out","both"].includes，否则 400）+ depth 下限钳与取整（Math.max(1, Math.floor(…))）。
3. `/api/update-check` 版本比较改分量比较（现字典序，"0.46.9" vs "0.46.10" 误报）。
4. content.ts enrichDeletedAt 的 N+1（回收站 100 行至多 100 个并发 get_page）——考虑仅对已删行补齐或找专用 op。
5. 编排/集成类测试超时放宽（app.test/orchestrator/content versions 用例在满载机器偶发 5s 超时）。
6. facts 路由 limit 的 NaN 守卫（同 /pages 的 numOr）。
7. pushEdge 去重键含 type（同端点异类型边折叠，纯视觉）。

## M4 主体范围（spec §7）

- 运维：请求日志、任务队列（jobs + SSE 实时）、Agents 与密钥管理复刻。
- 维护：备份（停机复制+保留策略+横幅倒计时）、配置只读页。
- 既有 backlog 不变项：discovery 死 key 回收、clearStaleLock 接线红线（先补宽限期+PID 复核）、chunk 证据列、tag/source 过滤 UI、frontmatter YAML 化。

## 接受现状（留痕）

- slug 字面以 /versions 结尾的歧义、search 不透传 include_deleted、entity 子串宽松匹配（fake 层）、计数非响应式 Map、双击双 expand、versions 串行 await、未知日期组置顶。

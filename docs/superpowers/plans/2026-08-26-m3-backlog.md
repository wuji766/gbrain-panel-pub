# M3 待办清单（M2 终审裁决产出 + M2 未尽规格项）

来源：m2-content 分支终审（2026-08-26）。M1 遗留的 M2 待办已消化（#1/#2/#3/#5/#6/#8 完成，#4 重入守卫仍欠），以下为 M3 开工时处理项。

## 遗留健壮性（M2 终审裁决"建议修"）

1. **orchestrator 重入守卫**（M1 遗留 #4，两轮顺延，勿再拖）：starting/own 态并发 start()、fallback 循环覆盖 `this.proc` → 旧子进程泄漏且无法 kill。双击 fallback 按钮即可触发。
2. **mcpCall 检查 `result.isError`**：当前只查 rpc 层 payload.error，真实 MCP 工具失败（HTTP 200 + content 内错误/isError）会假成功——前端 DELETE 不存在页弹"已软删除"。在 gbrain-client.ts 的 mcpCall 解包处统一检查并抛错，路由层自然转 502。
3. **q+type 组合失效**：GET /api/pages 在 q 有值时走 search 分支丢 type。真实 search 有 `types` 数组参数可映射（discovery 已核实）；修前 UI 先提示组合无效。
4. **NaN 守卫**：content.ts 的 `Number(query("limit"/"offset"))` 对非数字静默传 null；加 NaN 回退默认值。
5. fake get_page 无视 include_deleted（替身盲区，与 #6 相关）。

## M2 未尽的规格子项（spec §3，M2 计划显式缩减，M3 补齐或正式降级）

6. 页面库：chunk 证据列（search 返回的 chunk 前端丢弃）；tag/source_id 过滤 UI；过滤（q+type 组合）修正。
7. 页面详情：版本历史（page_versions 列表，spec §10 允许只列不 diff）；「恢复」按钮死 UI 修复——详情加载不传 include_deleted=true 导致已删页 deleted() 永假（当前恢复只能从回收站 tab 操作）。
8. 记忆库：kind/visibility 过滤（真实 recall 无此入参，需前端本地过滤）；supersessions 参数接入；分页（真实 recall limit cap=100，前端当前 limit=200 静默截断——**M3 必修**：分页或至少截断提示）。
9. 快速记事：kind 可选（spec 承诺"实体/类型可选"，当前只有实体）；README「可见性可选」文案与实现不符——补 UI 或改文档。
10. frontmatter 编辑换正式 YAML 解析（终审必修 1 已做最小透传保真；YAML 化顺带解决正文以 `---` 开头的误判边界）。

## 接受现状但留痕（M2 终审"接受"，M4 或随手）

11. PageDetail：取消编辑不重置表单；已删页可 PUT（未按 deleted 门控）；slug 双重 decodeURIComponent 冗余。
12. Pages.vue：search 无 total 时退化本页数；整倍数页边界空页；回收站 limit=100 无分页；无在途请求守卫。
13. Capture：Ctrl+Enter 无 submitting 守卫（可重复提交）。
14. fake：fail 消息风格不统一（"not found"/"not_found"）；search 空 query 返回全部。
15. Bun panic 修复的 arrayBuffer() 全量读 body——Bun 上游修复后重验（回归测试已有）。
16. 未知 /api 路径落 catch-all 返回 SPA 200（M1 预存在）。
17. app.test.ts boot 类测试满载偶发超时（重跑即过）。

## 红线（不变）

- clearStaleLock 接线前必须补空目录宽限期 + rm 前 PID 复核（M1 待办 #9）。
- discovery 每次 2 把死 key：M4 key 管理时回收。
